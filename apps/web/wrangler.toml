#:schema node_modules/wrangler/config-schema.json
name = "q3ik-mail"
main = ".open-next/worker.js"
compatibility_flags = ["nodejs_compat"]
compatibility_date = "2025-08-16"

# Static assets produced by @opennextjs/cloudflare
# The binding name MUST be "ASSETS" for the cache to work.
[assets]
directory = ".open-next/assets"
binding = "ASSETS"

# D1 database binding
[[d1_databases]]
binding = "DB"
database_name = "q3ik-mail-db"
database_id = "REPLACE_WITH_YOUR_DATABASE_ID"

[[r2_buckets]]
binding = "EMAIL_BODIES"
bucket_name = "q3ik-mail-bodies"

[vars]
# RESEND_API_KEY is set via: wrangler secret put RESEND_API_KEY
# In production the real secret (set via `wrangler secret put`) overrides this
# dev/CI fallback. The value here only applies to local `wrangler dev` and CI.
THREAD_LIST_CURSOR_SECRET = "dev-cursor-secret"
