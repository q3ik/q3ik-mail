{
  "name": "q3ik-mail",
  "private": true,
  "packageManager": "pnpm@10.10.0",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "deploy": "wrangler pages deploy apps/web/out"
  },
  "devDependencies": {
    "@types/node": "^25.6.2"
  }
}
