{
  "name": "inbox-q3",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "deploy": "wrangler pages deploy apps/web/out"
  },
  "devDependencies": {
    "@types/node": "^25.6.2"
  }
}
